import styled from "@emotion/styled";

const Wrapper = styled.div`
  background-color: rgb(162, 203, 229);
  min-width: 25rem;
`;

export default function LayoutSidebar() {
  return <Wrapper>사이드바 영역</Wrapper>;
}
