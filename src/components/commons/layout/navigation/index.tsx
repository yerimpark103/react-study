import styled from "@emotion/styled";

const Wrapper = styled.div`
  background-color: rgb(231, 172, 82);
  min-height: 5rem;
`;

export default function LayoutNavigation() {
  return <Wrapper>네비게이션 영역</Wrapper>;
}
