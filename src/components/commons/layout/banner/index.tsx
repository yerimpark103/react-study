import styled from "@emotion/styled";

const Wrapper = styled.div`
  background-color: rgb(235, 187, 193);
  min-height: 15rem;
`;

export default function LayoutBanner() {
  return <Wrapper>배너 영역</Wrapper>;
}
